# 工业多轮对话 AI Agent 整体规划与总结

## 一、目标定位

构建一个**本地/私有化部署的工业数据分析 Agent**，支持多轮自然语言对话，具备以下核心能力：

- 数据探索（EDA）
- 相关性分析
- 数据清洗与基础特征工程
- 建模
- 模型结果分析
- 模型解释性分析（如特征重要性、SHAP 等）
- 结果以结构化表格 + 图片形式在对话界面中展示

**非目标（第一阶段明确不做）**：通用多模态、实时流式数据、复杂时序预测平台、完全替代专业数据科学家。优先做成「表格型结构化数据的探索-建模-解释」闭环。

## 二、最终形态

```
用户（自然语言多轮对话）
        ↓
Dify Agent / Chatflow
  ├── 系统提示词（角色、工具使用规范、状态优先原则）
  ├── 会话记忆（conversation_id 贯穿）
  └── 工具选择与结果解释
        ↓
自研 Tools 服务（FastAPI，可一个或多个进程）
  ├── EDA、相关性、建模、解释性等独立 Tool（每个 API 端点 = 一个 Tool）
  ├── 统一通过 conversation_id 定位工作区
  └── 返回结构化 JSON + 图片可访问 URL
        ↓
结果在 Dify 对话界面展示（Markdown 表格 + 图片）
```

**核心原则**：
- LLM 只负责理解意图、选择工具、解释结果，不负责真实计算。
- 所有确定性计算、文件读写、模型训练均由 Tools 完成。
- 一个 FastAPI 服务可挂载多个 Tool；重计算工具后续可独立拆分。

## 三、架构决策总结

### 3.1 技术选型
- **编排与对话层**：Dify（已 Docker 本地部署，支持本地 Ollama + 云端模型）
- **工具层**：FastAPI + Pydantic（现有 EDA 模式继续扩展）
- **工具注册方式**：OpenAPI Schema 导入 Dify（已跑通）
- **数据存储**：本地文件系统 `workspace/{conversation_id}/`
- **可视化交付**：工具生成图片后通过静态文件服务返回 HTTP URL（Dify 直接渲染）

### 3.2 会话与文件管理（关键共识）
- **session_id = Dify 的 conversation_id**（直接复用，不自己生成）
- 目录结构（通用，不按任务类型再分层）：
  ```
  workspace/
  └── {conversation_id}/
      ├── raw/          # 用户上传或 SQL 查询落地的原始数据
      ├── processed/    # 清洗、转换后的数据
      ├── plots/        # 所有图片
      ├── results/      # 模型文件、矩阵、摘要等
      └── meta.json     # 会话状态摘要（当前数据、已执行步骤等）
  ```
- **数据引入铁律**：会话内文件最初来源必须是用户动作（上传文件 或 提供 SQL/查询意图）。Agent 不得凭空创造数据。
- **生命周期**：先有 conversation_id，再有该目录下的一切内容。用户删除会话后，通过定时任务 + 可选主动清理工具删除对应 workspace。

### 3.3 数据流
1. 用户上传文件或说出查询需求
2. Dify 仅传递 `conversation_id` + 相对路径（或 SQL）
3. FastAPI 在本地磁盘完成读写与计算
4. 返回 JSON 结果 + 图片 URL
5. Agent 解释并展示

**禁止**把整个数据集通过 HTTP Body 传递。

### 3.4 工具设计原则
- 继承统一的 `ToolRequestBase` / `ToolResponseBase`
- 每个 Tool 职责单一，描述写清楚「何时使用、输入约束、输出含义」
- 重任务（建模、SHAP）预留异步化接口
- 所有路径必须校验 conversation_id，防止穿越

## 四、分阶段实施规划

### 阶段 0：基础加固（当前可立即做）
- 确认 conversation_id 能稳定传入工具
- 统一 workspace 初始化与路径校验逻辑
- 为 FastAPI 增加静态文件服务（/static/{conversation_id}/...）
- 补充简单的会话状态查询工具与清理工具

### 阶段 1：核心分析工具补齐
- 相关性分析工具（矩阵 + 热力图 + 显著对）
- 数据导入/注册工具
- 数据预览与采样工具
- 基础质量检查增强

### 阶段 2：建模与解释闭环
- 建模工具（sklearn 常见模型，返回指标 + 模型路径）
- 预测/评估工具
- 解释性工具（Permutation Importance、SHAP 摘要图等）
- 结果导出工具（打包当前会话关键产物）

### 阶段 3：工程化与体验
- 重任务异步化（提交 + 轮询）
- session 过期清理定时任务
- 完整系统提示词与工具描述打磨
- 日志、监控、错误码规范
- 考虑将 FastAPI 容器化，与 Dify 统一 docker-compose

### 阶段 4：扩展与生产
- 按需拆分重工具服务
- 支持更多数据源与格式
- 报告生成、权限、审计等企业特性

## 五、主要风险与应对

| 风险 | 应对 |
|------|------|
| 长时任务超时 | 异步任务模式 + 进度查询 |
| LLM 工具调用不稳定（尤其本地小模型） | 强模型调试 + 详细工具描述 + 回归测试 |
| 图片无法在 Dify 显示 | 静态 URL + CORS 验证优先于 base64 |
| workspace 膨胀 | 定时清理 + 主动清理工具 |
| 路径安全 | 严格 conversation_id 校验 + 禁止 .. |
| 单进程阻塞 | 后期拆服务或加队列 |
| 中间状态丢失 | meta.json + 状态查询工具强制使用 |

## 六、可行性验证思路（最小化实现路径）

目标：用最小代价验证「多轮对话 + 工具调用 + 文件隔离 + 结果可视化」全链路是否通畅，再决定是否投入完整开发。

### 验证步骤（建议 1–2 天内完成）

1. **静态文件服务验证**  
   在现有 FastAPI 中增加 StaticFiles，手动放一张图片到 `workspace/test-id/plots/test.png`，确认通过 `http://host.docker.internal:8000/static/test-id/plots/test.png` 可访问。

2. **conversation_id 传递验证**  
   修改现有 EDA 工具，强制打印/返回收到的 session_id，在 Dify 中调用，确认能拿到真实的 conversation_id。

3. **最小相关性工具**  
   实现一个极简版本：只接收 conversation_id + dataset_path，计算数值列相关性，生成热力图并保存，返回矩阵摘要 JSON + 图片 URL。不追求完美，只求跑通。

4. **Dify Agent 最小配置**  
   - 注册 EDA + 新的相关性工具  
   - 系统提示词写死：「必须使用工具获取真实结果，优先使用当前 conversation 的数据」  
   - 用同一个对话先调 EDA，再调相关性，观察是否能正确传递路径并展示图片。

5. **文件隔离与清理验证**  
   开两个对话，分别上传不同文件，确认互不干扰；手动删除一个 workspace 目录，观察是否影响另一个。

6. **失败路径验证**  
   故意传不存在的文件、错误列名，确认工具返回结构化 error，Agent 能向用户解释而不是崩溃。

**通过标准**：
- 同一会话内多轮工具调用能正确复用数据
- 图片能在 Dify 对话中显示
- 不同会话数据隔离
- 错误可被优雅处理

全部通过后，再按阶段 1–2 正式扩展工具箱；任何一步失败，优先把基础设施问题（网络、路径、conversation_id、静态服务）解决，而不是急着加新功能。

---
